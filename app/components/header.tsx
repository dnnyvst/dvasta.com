import { ThemeToggle } from "@/components";

export const Header = () => {
  return (
    <header className="flex justify-between">
      <h1>danny vasta</h1>
      <ThemeToggle />
    </header>
  );
};
